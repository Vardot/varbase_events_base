# Node tooling + varbase-e2e install (installed on demand, not committed).
/node_modules/
/package-lock.json
/yarn.lock
/.yarn/
/.yarnrc.yml
/.pnp.*

# varbase-e2e / Playwright run artifacts.
/tests/reports/
/tests/screenshots/
/tests/videos/

# CI-assembled test site + caches.
/.site/
/.cache/
/events-base-db.sql*
/.gitlab-ci-local/
